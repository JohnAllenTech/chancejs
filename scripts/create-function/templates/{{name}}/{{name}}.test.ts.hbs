import { {{name}} } from './{{name}}'

describe('Testing {{pascalCase name}} {{pascalCase name}} function', () => {
  it('returns an random {{name}}', () => {
    const result = {{name}}()
    expect(typeof result).toEqual('{{returnType}}')
  })
})
